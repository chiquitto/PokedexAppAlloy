var args = arguments[0] || {};

// Ti.API.log(JSON.stringify($.type.typeId));
$.type.typeId = args.type.getId();
// Ti.API.log(JSON.stringify($.type.typeId));

// $.label.setText(args.damageFactor + 'x');

// $.weaknessesCell. ['weaknessesCell'].concat(args.classes)